language: python
python:
  - "3.7"
  - "3.8"
  - "3.9"
before_install:
  - sudo rm -f /etc/boto.cfg
  - export BOTO_CONFIG=/dev/null
install:
  - pip install -r requirements/test-requirements.txt
script:
  - black --check .
  - isort --check .
  - make run-tests
